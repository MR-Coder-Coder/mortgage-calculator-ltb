# This file was auto-generated (partially)

name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install
      - run: |
          unset CI
          npm run build
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      - name: Login to Firebase
        run: firebase login --ci  # Use CI flag for automated login
      - name: Deploy Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0  # Keep using this action for hosting
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MORTGAGE_CALCULATOR_F9283 }}
          channelId: live
          projectId: mortgage-calculator-f9283
      - name: Deploy Firestore and Cloud Functions
        run: firebase deploy --only firestore,functions  # Deploy Firestore and Functions with CLI